<?xml version="1.0" encoding="utf-8"?>
<Application xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Encounters.App">
    <Application.Resources>
        <Thickness x:Key="PageMargin">20</Thickness>

        <!-- Colors -->
        
        <Color x:Key="NotSoLightBlueColour">#2A7886</Color>
        <Color x:Key="LightBlueColour">#79BAC1</Color>
        <Color x:Key="GhostColour">AliceBlue</Color>
        <Color x:Key="AubergineColour">#512B58</Color>
        
        <Color x:Key="AppBackgroundColor">AliceBlue</Color>
        <Color x:Key="NavigationBarColor">#1976D2</Color>
        <Color x:Key="NavigationBarTextColor">White</Color>

        <!-- Implicit styles -->
        <Style TargetType="{x:Type NavigationPage}">
            <Setter Property="BarBackgroundColor"
                    Value="{StaticResource NotSoLightBlueColour}" />
            <Setter Property="BarTextColor"
                    Value="{StaticResource GhostColour}" />           
        </Style>

        <Style TargetType="{x:Type ContentPage}"
               ApplyToDerivedTypes="True">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource GhostColour}" />
        </Style>
    </Application.Resources>
</Application>